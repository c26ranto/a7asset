enum Mode { add, edit, view, approval, confirm, verify }

enum SaveMode {
  view,
  insert,
  edit,
  delete,
  approve,
  unapprove,
  reject,
  confirm,
  unconfirm
}

// TODO INPUT TYPE
enum InputType { a, b, c }

enum PostRequestType { body, formdata, none }

enum CardStatusType {
  total,
  done,
  notDone,
}

enum TakePhotoChecklistType { dialog, detail }

enum SaveChecklistType { dialog, page }
